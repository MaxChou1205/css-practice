# Init Project(page)

1. clone template
   `degit MaxChou1205/css-practice/templat ./pages/<project name>`

2. init project
   `yarn`

3. run preview
   `yarn dev`

4. run build
   `yarn build`
